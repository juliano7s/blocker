
# Bugs Units
 - Soldier explosion not working
 - Blocks are never giving up to go to the target cell. If multiple blocks get the same target cell, only one will reach it. The others should give up after some ticks.
 - Magnet Pull in the warden is not working properly. It should pull all blocks in the zone as close as possible to the warden. Now it's pulling only one block closer.
 - Jumper cooldown is not working as designed. It's getting immobile cooldown for every jump.
 - Jumper jump is triggered immediatelly after typing F. It should just enable the selection of where to jump
 - Jumper should jump up to 5 cells

# Bugs UI
 - The hud info bar at the top is not showing everything, each item is too wide apart from each other.